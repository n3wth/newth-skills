# Supabase (auth, upvotes, comments)
# Get from: https://supabase.com/dashboard → project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Neon / Postgres (anonymous votes, analytics)
# Vercel: DATABASE_URL, POSTGRES_URL, or SKILLS_POSTGRES_URL (with custom prefix)
DATABASE_URL=postgresql://...
# Or: POSTGRES_URL, SKILLS_POSTGRES_URL (Vercel Supabase integration with prefix)

# AI (optional - for task recommendations)
GEMINI_API_KEY=

# Sentry (optional - error tracking)
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# E2E (optional)
BASE_URL=http://localhost:3000
