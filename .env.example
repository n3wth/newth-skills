# Supabase (auth, upvotes, comments)
# Get from: https://supabase.com/dashboard → project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Neon / Postgres (anonymous votes, analytics)
# Vercel integrates Neon automatically; or set manually
DATABASE_URL=postgresql://...
# Alternative: POSTGRES_URL, or PGHOST + PGUSER + PGPASSWORD + PGDATABASE

# AI (optional - for task recommendations)
GEMINI_API_KEY=

# Sentry (optional - error tracking)
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# E2E (optional)
BASE_URL=http://localhost:3000
